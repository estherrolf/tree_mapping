# tree_mapping

# prepare data.
1. aggregate lidar data by site (1m).
2. aggregate all data products and coarsen to 10m resolution. 


# evaluating existing data products
First run `prepare_tch_reference.ipynb`.

Then run `eval_existing_tch.ipynb`.
